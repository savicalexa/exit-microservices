apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  namespace: festival-system
  labels:
    app: payment-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: payment-service
  template:
    metadata:
      labels:
        app: payment-service
    spec:
      containers:
        - name: payment-service
          image: ghcr.io/replace-me/festival-payment-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8084
          env:
            - {name: EUREKA_URL, value: "http://service-discovery:8761/eureka/"}
            - name: MONGODB_URI
              valueFrom: {secretKeyRef: {name: festival-secrets, key: mongodb-uri}}
            - {name: RABBITMQ_HOST, value: rabbitmq}
            - {name: RABBITMQ_USERNAME, value: festival}
            - name: RABBITMQ_PASSWORD
              valueFrom: {secretKeyRef: {name: festival-secrets, key: rabbitmq-password}}
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: http
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: http
            initialDelaySeconds: 45
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 768Mi
---
apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: festival-system
spec:
  selector:
    app: payment-service
  ports:
    - name: http
      port: 8084
      targetPort: http
  type: ClusterIP
